<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SousChef"
    xmlns:local2="using:SousChef.Controls">

    <x:Double x:Key="myHeight2">100</x:Double>
    <Style TargetType="local2:TestingControl" >        
        <Setter Property="Template">
            <Setter.Value>                                
                <ControlTemplate TargetType="local2:TestingControl">

                    <Border
                        Height="{StaticResource myHeight2}"
                        Background="Green"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel>
                            <TextBlock Text="{Binding Content.myText, RelativeSource={RelativeSource Self}}"/>
                            <ContentPresenter Content="{TemplateBinding Content}"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
